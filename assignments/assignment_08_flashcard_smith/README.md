## Assignment 8: Flashcard Smith

Concept focus: Pydantic structured output + batch generation

Scenario
- Teachers want quick flashcards from short topic blurbs.

Your tasks (in `flashcard_smith.py`)
- Define a `Flashcard` model with `term`, `definition`, `difficulty`.
- Accept a list of blurbs and produce flashcards via `.batch()`.
- Print a compact table of results.

Run
```bash
python -m assignments.assignment_08_flashcard_smith.flashcard_smith
```


